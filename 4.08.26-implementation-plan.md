# Implementation Plan: Final Production Refinement [04.08.26]

## 1. UI & Aesthetic Refinement (Industrial Mono)
- [x] **Loading Screen**: Solid black background with centered animated `bull.512x512.png`. Removed legacy terminal text.
- [x] **Exit Intent Modal**:
    - Headline: 'OPERATIONAL LEAK DETECTION'
    - Framing: Resource-first, non-salesy.
    - Features: Email capture + PDF Download CTA + DEV trigger button.
- [x] **Hero Fix**: Verified `no-underline` on subtext spans in Spanish.
- [x] **FAQ Scaling**: Fixed 'OPERACIONES' text overrun in Spanish by adjusting header font sizes and adding `break-words`.

## 2. Technical Features & Funnel
- [x] **Forensic Schematic Overlay**: Implemented in `ArticleDetail.tsx` and `BlueprintProtocol.tsx`.
- [x] **Interactive Calculator Payload**:
    - Trigger: Success state now provides a 'Download Report' button.
    - Webhook: Configured to POST full breakdown JSON to Railway n8n instance.
    - Payload Schema: `total_leads`, `hours_saved`, `revenue_recovery`, `efficiency_multiplier`.
- [x] **Success Path**: Success state in calculator directs to Calendly Strategy Session.

## 3. Localization & Compliance
- [x] **Bilingual Consistency**:
    - Fixed Footer translation for "Book Strategy Session".
    - Standardized "US Domestic Spanish" across all new components.
    - Moved hardcoded strings in `BlueprintProtocol.tsx` to `i18n.ts`.
- [x] **Legal Architecture**:
    - Finalized `PrivacyPolicy.tsx` and `TermsConditions.tsx`.
    - Content: Authoritative, North American industrial data standards.
    - UI: Minimalist, no promotional bars.

## 4. Final Polish & Stability
- [x] **Dark Mode Default**: Enforced in `ThemeProvider`.
- [x] **Typography**: Standardized Inter/Outfit pairing with black-weight headers.
- [x] **Mobile Audit**: Verified responsive stacking for `ArticleDetail` and `Calculator`.

## 5. Next Steps for Handover
1. Verify CRM injection via webhook logs.
2. Direct user to review `ExitIntentModal` using the "DEV" floating button on Home.
3. Final build validation.
