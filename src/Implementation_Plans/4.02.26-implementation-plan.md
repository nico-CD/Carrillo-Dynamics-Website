# Implementation Plan - Hero & Funnel Adjustments (4.02.26)

This document outlines the changes to the landing page typography, hero section layout, and lead intake funnel routing.

## 1. Objectives
- Improve "above the fold" visibility of the hero CTA ("Get In Touch").
- Refine hero typography for better hierarchy and spatial efficiency.
- Remove the paid $99 audit step and redirect leads directly to a new success page.
- Update the intake form to collect target fields (Name, Email, Company, Website, Industry, Bottleneck).

## 2. Implementation History

### A. Typography & Hero Section Fixes
- [COMPLETED] Increased H1 font size (`text-4xl/8xl`) with tightened line-height (`0.85`).
- [COMPLETED] Scaled down subtext ("MANUAL DATA ENTRY...") with `whitespace-nowrap` for single-line fit.
- [COMPLETED] Adjusted hero top padding (`pt-32 md:pt-48`) for a more centered, authoritative feel.

### B. Intake Type Schema & Hooks
- [COMPLETED] Updated `src/types/intake.ts` with: `companyName`, `companyWebsite`, `industry`, `bottleneck`.
- [COMPLETED] Updated `src/hooks/useIntake.ts` to transmit new fields to the webhook payload.

### C. Intake Form Update (`src/pages/Index.tsx`)
- [COMPLETED] Headline changed to "**Get Your *Free Custom Blueprint.***" with `mx-auto` centering.
- [COMPLETED] Increased label contrast (`text-foreground`) and font size (`text-sm`).
- [COMPLETED] Updated consent checkbox naming to "**Blueprint Analysis**."
- [COMPLETED] Rerouted form submission directly to `/success`.

### D. Interactive Calculator (`src/components/InteractiveCalculator.tsx`)
- [COMPLETED] Adjusted logic: Minimum baseline floor of **30%** scaling up to a **100%** ceiling.

### E. Global Aesthetic Refinements
- [COMPLETED] Simplified "Stop the manual leak" to "**Stop the leak.**"
- [COMPLETED] Matched Bridge CTA button to high-contrast brand green (`#10b981`).
- [COMPLETED] Reordered sections: Calculator -> Bridge CTA -> Blueprint Protocol -> Form.
- [COMPLETED] Matched subtext styling in "Blueprint" section to "Sectors" typography (medium/muted-foreground).

### F. New Success Page (`src/pages/Success.tsx`)
- [COMPLETED] Built as centered glassmorphic card with Nico Carrillo signature and green CTA.

### G. Cleanup
- [COMPLETED] Removed `/audit` route from `App.tsx` and deleted legacy `src/pages/Audit.tsx`.

## 3. Final Verification Status
All critical adjustments and aesthetic refinements are live. The funnel is now centered on the personal "Video Blueprint" offer for higher conversion.
