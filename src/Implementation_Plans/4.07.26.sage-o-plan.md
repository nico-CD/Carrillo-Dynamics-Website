# SAGE-O Implementation Plan | 4.07.2026
## Strategy: Search, Answer, & Generative Engine Optimization
**Project Role**: Lead SEO/GEO Architect & Full-Stack Developer
**Base Domain**: `https://carrillodynamics.com`
**Deployment**: GitHub -> Coolify Automation

---

## 1. Overview
This plan establishes **Carrillo Dynamics** as a high-authority entity for both traditional search (SEO) and modern agentic systems (AEO/GEO). We are shifting from a static SPA to a data-dense, bilingual architecture that maximizes "Answerability" for AI models like GPT-4, Claude, and Perplexity.

---

## 2. Phase 1: Architectural Foundation (Bilingual + Metadata)
### [1.1] URL Subfolder Routing
Implement a robust bilingual structure using `react-router-dom`:
- `/en/*` (Default)
- `/es/*` (LatAm/Chicagoland Spanish Market)
- Automatic redirection from root based on browser locale (with manual override).

### [1.2] Dynamic Metadata (react-helmet-async)
Ensure every route injects a unique:
- `<title>` (Industrial Mono format: `ENTITY | ACTION.PAGE`)
- `<meta name="description">`
- `[hreflang]` tags for bilingual cross-linking.

---

## 3. Phase 2: Content Expansion (AEO/GEO Payload)
### [2.1] Forensic Articles (5)
Generate high-authority, "Industrial Mono" content for the following:
1. **The 60-Second Lead Rule**: Engineering near-zero friction for field service intake.
2. **Automating Chicagoland Home Services**: Localized systems for high-volume trade firms.
3. **AI vs. Traditional Dispatch**: Deterministic logic vs. human-error bottlenecks.
4. **Scaling without Hiring**: Reclaiming hours through engineered flow.
5. **The Forensic Lead Audit**: Detecting and eliminating operational leaks.

### [2.2] High-Intent FAQs (10)
Migrate to `src/data/faqs.json`. Add 4 new questions focusing on:
- Integration with legacy dispatch (Jobber, ServiceTitan).
- Data sovereignty and security protocols.
- ROI of custom engineering vs. SaaS subscriptions.
- Technical support and fractional operations oversight.

---

## 4. Phase 3: Technical SAGE-O (JSON-LD & Mapping)
### [3.1] ProfessionalService Schema
Inject JSON-LD on `index.html` and key pages:
- **Entity**: `ProfessionalService` -> Carrillo Dynamics.
- **Service Area**: `Chicagoland Area`.
- **SameAs Links**: LinkedIn, GitHub.
- **Offers**: Direct link to the "Blueprint Protocol" service.

### [3.2] Sitemap & Robots
- **Sitemap.xml**: Comprehensive bilingual mapping with `<xhtml:link rel="alternate" ... />`.
- **Robots.txt**: Explicitly ALLOW: `GPTBot`, `Claude-Web`, `Google-Extended`, `PerplexityBot`.

---

## 5. Phase 4: The Concept Car (Demo Gallery)
### [4.1] Internal Showpiece Shell
- **Route**: `/demo/*` (Internal mock architecture).
- **Component**: `DemoNavigator.tsx` (Hidden in production Nav, accessible via direct link/SEO).
- **Mock Screens**: Apex Response Center (Triage Dashboard), Field Crew HUD, Real-time Operations Blueprint.

---

## 6. Execution Constraints & Aesthetic
- **Brand Voice**: Industrial Mono (Minimalist, capitalized headers, technical precision).
- **Build Integrity**: Ensure `npm run build` succeeds locally before pushing to Coolify.
- **No Placeholders**: All mock screens must feel "Production Ready" even if they are purely UI demonstrations.

---

## 7. Deployment Checklist
- [x] Install `react-helmet-async`.
- [x] Migrate FAQs to `src/data/i18n.ts` (Structured AEO-dense).
- [x] Create Article Layout & Components (`Articles.tsx`, `ArticleDetail.tsx`).
- [x] Inject JSON-LD Schema (`StructuredData.tsx`).
- [x] Implement Bilingual Routing (`/:lang/` structure).
- [x] UI/UX Refinement & Theme Standardization (Universal Dark/Light mode).
- [x] Internal Demo Integration (Apex Response Center).
- [ ] Generate sitemap.xml & robots.txt (Pending Production URL finalization).
- [x] Final Build Audit (Passed).

---

## 8. Progress Audit (4.08.2026)
### Current Accomplishments:
1. **Universal Theme Compatibility**: All core routes (Index, Articles, FAQ, Apex Demo, Legal) have been migrated to theme-aware Tailwind tokens. Hardcoded black/zinc styles have been purged in favor of `bg-background` and `text-foreground`.
2. **Metadata Normalization**: Cleaned up `react-helmet-async` logic. Page titles now follow the professional `Carrillo Dynamics | [Page Name]` format instead of debug-style identifiers.
3. **Bilingual Integrity**: The `/en/` and `/es/` routing is fully functional across all components. Navigation, buttons, and legal pages correctly persist and switch language state without data loss.
4. **Apex Response Center (PREVIEW)**: The high-stakes triage demo is now integrated into the routing table and accessible via a subtle 'Terminal' trigger in the global Footer.
5. **Blueprint Protocol Polish**: The 'Forensic Lead Audit' section has been refined. Loom video embeds are now responsive and respect the theme-aware container architecture. The 'Command Center' CTA has been repositioned to the footer to prioritize the intake funnel.

### Next Technical Objectives:
---

## 10. Phase 5 Completion & Final Production Audit (4.08.2026)
### Summary of Final Polish:
1. **Automation Hardening**: Verified all `IntakeForm` fields use static English IDs (`lead_name`, `email`, `company_name`, etc.). This ensures the **n8n workflow** remains stable regardless of whether the user is viewing the site in English or Spanish.
2. **Bilingual FAQ & ROI Focus**: Rewrote all 10 FAQ entries from scratch in both languages. The content now targets high-intent pain points such as **speed-to-lead**, **operational chaos**, and **capacity scaling**, aligning with the "Forensic" brand narrative.
3. **Calendly Strategy Session**: Integrated the `CalendlyModal` globally. Strategy Session triggers are now embedded in the `Hero`, `BlueprintProtocol`, and every `ArticleDetail` page, providing a seamless, on-domain scheduling experience.
4. **Performance Architecture**: Lazy-loading and `Suspense` integrated at the routing layer. Initial bundle size minimized for faster perceived performance.
5. **UI Minimalisms**: Purged metadata (read times, labels) from article headers for a "Technical Documentation" aesthetic.
6. **Localized Logic**: The `InteractiveCalculator` is now fully bilingual, reactive, and uses translated templates for all ROI summaries.
7. **Global Navigation Polish**: Implemented a global `ScrollToTop` behavior to ensure consistent user orientation during internal navigation.

### Updated Checklist:
- [x] Install `react-helmet-async`.
- [x] Migrate FAQs to `src/data/i18n.ts` (Structured AEO-dense).
- [x] Create Article Layout & Components (`Articles.tsx`, `ArticleDetail.tsx`).
- [x] Inject JSON-LD Schema (`StructuredData.tsx`).
- [x] Implement Bilingual Routing (`/:lang/` structure).
- [x] UI/UX Refinement & Theme Standardization (Universal Dark/Light mode).
- [x] Internal Demo Integration (Apex Response Center).
- [x] Integrate Calendly Modal for direct strategy session scheduling.
- [x] Hardened Intake Form IDs for n8n automation safety.
- [x] Localized Interactive ROI Calculator.
- [ ] Generate sitemap.xml & robots.txt (Pending Production URL finalization).
- [x] Final Build Audit (Passed).

---

## 9. Phase 5: Final Production Polish & Automation Safety Audit (4.08.2026)
### [5.1] Critical Automation & Form Safety
- **Static Identifiers**: Assign immutable HTML `name` and `id` attributes to all Intake Form fields (e.g., `id="lead_name"`, `id="service_type"`) to prevent automation breakage during language toggling.
- **Mock Identity Update**: Change placeholder name from `Juan Perez` to `Juan Martinez`.

### [5.2] Navigation, Routing & UX Flow
- **Root Identity**: Standardize homepage title to `Carrillo Dynamics | Automation Agency`.
- **Reactive Translation**: Debug `LanguageProvider` to ensure instantaneous UI state propagation across all nested components upon toggle.
- **Focus Management**: Implement `window.scrollTo(0, 0)` on article selection to ensure the user landed at the header of the new content.
- **I18n Mapping completeness**: Verify and map `Privacy Policy`, `Terms`, and `Los Artículos` (Articles Nav) in `i18n.ts`.

### [5.3] UI/UX Cleanup & "Industrial Mono" Refinement
- **Article Visuals**: Purge "System Schematic" image placeholders and "Module Number" text from article headers.
- **Theme-Aware Sidebar**: Verify `ArticleSidebar` respects Dark/Light mode tokens correctly.
- **ROI Calculator Localization**: Wrap all calculator text and FTU (First-Time User) explanations in the `useTranslation` hook for bilingual support.
- **Typography Fix**: Remove the em-dash from the 'Forensic Audit' content section for cleaner visual flow.

### [5.4] High-Conversion Strategy Shift
- **Meeting Pivot**: Replace all "Request Audit" CTAs with a "Book Strategy Session" directive.
- **Direct Scheduling**: Integrate Calendly: `https://calendly.com/nico-carrillodynamics/30min`.
- **Anchor Consistency**: Ensure all "Get Free Blueprint" buttons utilize smooth-scroll logic to the primary intake form.

### [5.5] FAQ Hardening (Universal Service Focus)
- Update all 10 FAQ entries to focus on:
    - High-volume lead handling.
    - Speed-to-lead benchmarks.
    - System security and data protocols.
    - Tangible ROI in the trades/industrial sectors.
